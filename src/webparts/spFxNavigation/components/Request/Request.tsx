import { DefaultButton } from 'office-ui-fabric-react';
import * as React from 'react';

export default class Request extends React.Component {

    public render() {
        return(
            <DefaultButton text="Request"/>

        );
    }
}